---
title: Cartographier un codebase
slug: /cours/cartographier-codebase
---

import Link from '@docusaurus/Link';

# Cartographier un codebase

<div className="coursModuleMeta">
  <span>20–30 min</span>
  <span>GitHub Copilot</span>
  <span>Code existant</span>
</div>

<p className="coursModuleLead">
Avant de demander à l’IA de modifier un projet, il faut comprendre où intervenir. Dans ce module, on apprend à repérer les fichiers, responsabilités et risques importants sans lire tout le codebase.
</p>

## Le problème

Voici une demande fréquente :

```text
Ajoute cette fonctionnalité dans le projet.
```

Sans cartographie, l’IA risque de :

- modifier un fichier secondaire;
- ignorer l’architecture existante;
- dupliquer une logique déjà présente;
- proposer une solution qui fonctionne localement, mais qui ne respecte pas le projet;
- créer un changement difficile à réviser.

## Ce qu’il faut comprendre

Cartographier un codebase, ce n’est pas tout lire.

C’est repérer :

1. le point d’entrée probable;
2. les fichiers liés;
3. les responsabilités principales;
4. les dépendances ou appels importants;
5. les endroits à ne pas toucher.

La bonne question n’est pas :

```text
Est-ce que je comprends tout le projet?
```

La bonne question est :

```text
Est-ce que je comprends assez pour faire cette modification sans deviner?
```

## Exemple guidé

Dans un petit projet existant, on peut commencer par demander à l’IA :

```text
Avant de modifier le code, aide-moi à identifier les fichiers probablement concernés par cette fonctionnalité.
Explique brièvement le rôle de chaque fichier et ce qu’il faudrait vérifier avant de changer quoi que ce soit.
```

À observer :

- les fichiers proposés;
- les hypothèses de l’IA;
- les zones d’incertitude;
- les fichiers à ouvrir avant modification.

:::tip
Une cartographie n’est pas une preuve. C’est une hypothèse de travail à vérifier avant de modifier le code.
:::

## À toi de jouer

On veut faire ce changement :

```text
Ajouter un message d’erreur personnalisé quand le formulaire est invalide.
```

Avant toute modification, produis :

- une liste de fichiers probablement concernés;
- le rôle supposé de chaque fichier;
- une question à poser à l’IA pour confirmer la zone de modification;
- un risque si on modifie directement sans cartographier.

## À retenir

- Cartographier, ce n’est pas lire tout le projet.
- L’objectif est de réduire les hypothèses.
- L’IA peut aider à cartographier, mais il faut vérifier.
- Une bonne cartographie rend les modifications plus petites et plus révisables.

## Pour aller plus loin

- **Essentiel** : retrouver le fichier ou la zone probable à modifier.
- **Consolidation** : lire les dépendances, symboles ou appels liés à la zone ciblée.
- **Défi** : comparer une lecture manuelle avec un outil de cartographie.

## Navigation

<div className="coursActions">
  <Link className="button button--secondary" to="/cours/contexte-utile">
    Retour : contexte utile
  </Link>
  <Link className="button button--primary" to="/cours/verifier-reponse-ia">
    Continuer : vérifier une réponse IA
  </Link>
</div>

<details className="coursPedagoRefs">
  <summary>Références pédagogiques</summary>

  - Module : `MIC-MAP-001`
  - Compétences : `CMP-MAP-001`, `CMP-EVA-001`
  - Source : `project/current/cours-ia-avance/modules/MIC-MAP-001-cartographier-codebase.md`
  - Statut : module pilote publié
  - Équivalences utiles : Codex, Claude Code, CodeGraph ou outil équivalent lorsque disponible

</details>
