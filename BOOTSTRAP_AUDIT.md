# Bootstrap Audit

## Source

- Gabarit initial consigné dans la section À propos de la trousse.

## État local

- Copie locale indépendante sous `P:\Projects\relCoderAvecIA`.
- Aucune génération du contenu REL final n’a été faite.
- L’application Docusaurus existante sous `web/` a été conservée.

## Validation

- `npm run build` dans `web/` fonctionne.
- `npm run typecheck` est disponible dans `web/`.
- Le dépôt racine est initialisé localement avec `git init`.

## Reste à faire manuellement

- Remplacer les placeholders GitHub par le futur compte ou dépôt réel si le projet doit publier des liens d’édition.
- Ajouter progressivement le contenu REL dans la structure Markdown du site.
